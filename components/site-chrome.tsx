"use client";
import {useState} from "react";import Link from "next/link";import Image from "next/image";import {Menu,X,ArrowRight} from "lucide-react";import {site} from "@/lib/site";
const nav=[["Home","/"],["About","/about"],["Programs","/programs"],["Research","/research"],["Consultancy","/consultancy"],["Partnerships","/partnerships"],["Gallery","/gallery"],["Contact","/contact"]] as const;
export function Logo(){return <Link href="/" className="logo" aria-label={`${site.name} home`}><Image src="/images/logo/deepsom-logo.png" alt={site.fullName} width={656} height={338} priority/></Link>}
export function Navbar(){const [open,setOpen]=useState(false);return <header className="nav-wrap"><nav className="shell nav" aria-label="Main navigation"><Logo/><div className="links">{nav.map(([label,href])=><Link key={href} href={href}>{label}</Link>)}</div><Link className="btn btn-primary nav-cta" href="/partnerships">Partner With Us <ArrowRight size={16}/></Link><button className="menu" aria-expanded={open} aria-label="Toggle navigation" onClick={()=>setOpen(value=>!value)}>{open?<X/>:<Menu/>}</button></nav>{open&&<div className="shell mobile-links">{nav.map(([label,href])=><Link key={href} href={href} onClick={()=>setOpen(false)}>{label}</Link>)}</div>}</header>}
export function Footer(){return <footer className="footer"><div className="shell footer-grid"><div><Logo/><p>Advancing responsible artificial intelligence education, research and innovation across Somaliland, the Horn of Africa and beyond.</p></div><div><h3>Explore</h3>{nav.map(([label,href])=><Link key={href} href={href}>{label}</Link>)}</div><div><h3>Get in touch</h3><a href={`mailto:${site.email}`}>{site.email}</a><p>{site.address}</p><Link href="/contact">Contact Deepsom AI</Link></div></div><div className="shell fine">© {new Date().getFullYear()} {site.fullName}. All rights reserved.</div></footer>}
