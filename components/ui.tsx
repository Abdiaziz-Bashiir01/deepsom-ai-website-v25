"use client";
import Link from "next/link";import {ArrowRight,BookOpen,BrainCircuit,Building2,CloudCog,Cpu,Globe2,Handshake,Landmark,Leaf,LockKeyhole,Microscope,Network,Quote,ShieldCheck,Sparkles,University,Users} from "lucide-react";import {motion} from "framer-motion";import type {IconName} from "@/content/site-content";
const icons:Record<IconName,React.ComponentType<{size?:number}>>={brain:BrainCircuit,cpu:Cpu,network:Network,sparkles:Sparkles,cloud:CloudCog,lock:LockKeyhole,leaf:Leaf,shield:ShieldCheck,book:BookOpen,globe:Globe2,microscope:Microscope,users:Users,quote:Quote,university:University,landmark:Landmark,building:Building2,handshake:Handshake};
export function SectionHead({eyebrow,title,children}:{eyebrow:string,title:string,children?:React.ReactNode}){return <div className="section-head"><span className="eyebrow">{eyebrow}</span><h2>{title}</h2>{children&&<p>{children}</p>}</div>}
export function Card({title,text,icon,children}:{title:string,text:string,icon?:IconName,children?:React.ReactNode}){const Icon=icon?icons[icon]:undefined;return <motion.article className="card" initial={{opacity:0,y:18}} whileInView={{opacity:1,y:0}} viewport={{once:true,amount:.15}} transition={{duration:.45}} whileHover={{y:-5}}>{Icon&&<span className="icon"><Icon size={22}/></span>}<h3>{title}</h3><p>{text}</p>{children}</motion.article>}
export function Action({href="/contact",children}:{href?:string,children:React.ReactNode}){return <Link className="btn btn-primary" href={href}>{children}<ArrowRight size={16}/></Link>}
export function Outcomes({items}:{items:string[]}){return <ul className="list">{items.map(x=><li key={x}>{x}</li>)}</ul>}
