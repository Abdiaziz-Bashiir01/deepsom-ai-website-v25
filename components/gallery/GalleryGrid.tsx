"use client";
import {useMemo,useState} from "react";import {galleryCategories,type GalleryItem} from "@/content/gallery";import {GalleryCard} from "./GalleryCard";import {GalleryLightbox} from "./GalleryLightbox";
export function GalleryGrid({items}:{items:GalleryItem[]}){const [category,setCategory]=useState("All");const [selected,setSelected]=useState<GalleryItem|null>(null);const visible=useMemo(()=>category==="All"?items:items.filter(item=>item.category===category),[category,items]);return <><div className="gallery-filters" aria-label="Gallery categories">{["All",...galleryCategories].map(value=><button className={category===value?"active":""} onClick={()=>setCategory(value)} key={value}>{value}</button>)}</div>{visible.length?<div className="gallery-grid">{visible.map(item=><GalleryCard item={item} key={item.src} onOpen={()=>setSelected(item)}/>)}</div>:<p className="gallery-empty">This category will be published when its photographs are verified and added by Deepsom AI.</p>}{selected&&<GalleryLightbox item={selected} onClose={()=>setSelected(null)}/>}</>}
