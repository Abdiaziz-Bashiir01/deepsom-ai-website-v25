"use client";
import {useEffect} from "react";import Image from "next/image";import {X} from "lucide-react";import type {GalleryItem} from "@/content/gallery";
export function GalleryLightbox({item,onClose}:{item:GalleryItem;onClose:()=>void}){useEffect(()=>{const close=(event:KeyboardEvent)=>event.key==="Escape"&&onClose();window.addEventListener("keydown",close);return()=>window.removeEventListener("keydown",close)},[onClose]);return <div className="lightbox" role="dialog" aria-modal="true" aria-label="Gallery image preview" onClick={onClose}><button className="lightbox-close" aria-label="Close image preview" onClick={onClose}><X/></button><div className="lightbox-image" onClick={event=>event.stopPropagation()}><Image src={item.src} alt={item.alt} fill sizes="90vw"/></div></div>}
